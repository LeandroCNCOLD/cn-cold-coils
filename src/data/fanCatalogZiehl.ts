// DESATIVADO: o catálogo ZIEHL-ABEGG foi consolidado em fanCatalog.ts
// (versão única com separação 50/60 Hz). Mantemos export vazio para
// preservar compatibilidade de importação.

import type { FanModel } from './fanCatalogTypes';

export const FAN_CATALOG_ZIEHL: FanModel[] = [];
